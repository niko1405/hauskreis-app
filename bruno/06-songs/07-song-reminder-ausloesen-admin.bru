meta {
  name: Song-Reminder auslösen (admin)
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/hauskreise/{{hauskreisId}}/songs/reminders
  body: none
  auth: inherit
}

tests {
  test("antwortet mit 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
