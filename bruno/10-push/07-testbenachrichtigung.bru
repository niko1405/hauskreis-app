meta {
  name: Testbenachrichtigung
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/push/test
  body: none
  auth: inherit
}

tests {
  test("antwortet mit 201", function () {
    expect(res.getStatus()).to.equal(201);
  });
}

docs {
  Geht nur an die eigenen Geräte und landet nicht im Log.
}
