meta {
  name: Fremde Abwesenheit löschen (admin)
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/hauskreise/{{hauskreisId}}/absences/{{otherAbsenceId}}
  body: none
  auth: inherit
}

tests {
  test("antwortet mit 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
