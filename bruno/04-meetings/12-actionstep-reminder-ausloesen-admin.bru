meta {
  name: Actionstep-Reminder auslösen (admin)
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/hauskreise/{{hauskreisId}}/meetings/actionstep-reminders
  body: none
  auth: inherit
}

tests {
  test("antwortet mit 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
